<h1>adminリストに追加</h1>

<%= form_for [:admin, @artist] do |k| %>

　<h3>アーティスト</h3> <%= k.text_field :artist_id %><%= k.submit '新規追加' %>

<% end %>

<%= form_for [:admin, @label] do |k| %>

　<h3>レーベル</h3> <%= k.text_field :label_id %><%= k.submit '新規追加' %>

<% end %>

<%= form_for [:admin, @genre] do |k| %>

　<h3>ジャンル</h3> <%= k.text_field :genre_id %><%= k.submit '新規追加' %>

<% end %>