
<div class="container">
   <div class="row">
        <div class="col-md-10">

			<h1>adminリストに追加</h1>
			<span class="label label-danger pull-right" style="font-weight: bold; font-size: 15px;" ><%= link_to"更新完了", new_admin_cd_path %></span>


			<%= form_for [:admin, @label] do |k| %>
			　<h3>レーベル</h3> <%= k.text_field :label_name %><%= k.submit '新規追加' %>
			<% end %>
			<select name="">
				<% @labels.each do |label|%>
				<option value=""><%= label.label_name %></option>
				<% end %>
			</select>
<hr>

			<%= form_for [:admin, @genre] do |k| %>
			　<h3>ジャンル</h3> <%= k.text_field :genre_name %><%= k.submit '新規追加' %>
			<% end %>
			<select name="">
				<% @genres.each do |genre|%>
				<option value=""><%= genre.genre_name %></option>
				<% end %>
			</select>
    <hr>
			<%= form_for [:admin, @artist] do |artist| %>
			　<h3>アーティスト</h3> <%= artist.text_field :artist_name %>
			 <%= artist.submit '新規追加' %>
			<% end %>
			<select name="">
				<% @artists.each do |artist|%></a>
				<option value=""><%= artist.artist_name %></option>
				<% end %>
			</select>
            <hr>
             <h3>レーベル</h3>
			<% @labels.each do |label|%>
			　<td><%= label.label_name %>
			　<%= link_to "削除", admin_label_path(label), method: :delete %></td>
			<% end %>
			<hr>
			  <h3>ジャンル</h3>
			    <% @genres.each do |genre|%>
			      <td><%= genre.genre_name %>
			      <%= link_to "削除", admin_genre_path(genre), method: :delete %></td>
			    <% end %>
			<hr>
			<h3>アーティスト</h3>
			<% @artists.each do |artist|%></a>
			  <td><%= artist.artist_name %>
			  <%= link_to "削除", admin_artist_path(artist), method: :delete %></td>
			<% end %>
		</div>
	</div>
</div>