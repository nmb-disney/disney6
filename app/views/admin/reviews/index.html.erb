<div class="container">
   <div class="row">
        <div class="col-xs-3">
		  <%= render 'admin/users/sidebar' %>
        </div>


		<div class="col-xs-9">
		  <h1>adminレビュー一覧</h1>
		  	<table class="table table-bordered">
				<tr class="active">
	              <th>ID</th>
	              <th>名前</th>
	              <th>フリガナ</th>
	              <th>CDタイトル</th>
	              <th>レビュー内容</th>
	              <th> </th>
	            </tr>
	            <tr>
			    <% @reviews.each do |r| %>
			    <tr class="active">
			      <td class="active"><%= r.user.id %></td>
				  <td class="active"><%= r.user.lastname %><%= r.user.firstname %></td>
	              <td class="active"><%= r.user.lastname_kana %><%= r.user.firstname_kana %></td>
				  <td class="active"><%= r.cd.cd_title %></td>
				  <td class="active"><%= r.comment %></td>
				  <td class="active"><%= link_to "削除", admin_review_path(r), method: :delete, data: {confirm: '本当に削除してもいいですか？'} %></td>
				</tr>
			    <% end %>
            </table>
		</div>
    </div>
</div>
<div class="pagiWrapper">
 <%= paginate @reviews %>
</div>